# Database connection string
# For SQLite (default for development)
DATABASE_URI=file:./question-payloadcms.db

# For PostgreSQL (recommended for production)
# DATABASE_URI=postgresql://username:password@localhost:5432/question-app

# For Supabase PostgreSQL (example)
# DATABASE_URI=postgresql://postgres.xxx:password@aws-0-region.pooler.supabase.com:5432/postgres

# Used to encrypt JWT tokens (REQUIRED)
# Generate a secure random string: openssl rand -base64 32
PAYLOAD_SECRET=your-32-character-secret-key-here

# Used to configure CORS, format links and more (REQUIRED)
# No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# For production, use your domain:
# NEXT_PUBLIC_SERVER_URL=https://your-domain.com

# Secret used to authenticate cron jobs
CRON_SECRET=your-cron-secret-here

# Used to validate preview requests
PREVIEW_SECRET=your-preview-secret-here

# Node.js environment
NODE_ENV=development

# Disable Next.js telemetry (optional)
NEXT_TELEMETRY_DISABLED=1

# Port for the application (optional, defaults to 3000)
PORT=3000

# Additional security settings for production
# PAYLOAD_CONFIG_PATH=src/payload.config.ts